#!/usr/bin/env -S bash -e
#
# Check that OS dependencies are in place
# This script is a hook for appspec.yml and AWS CodeDeploy

echo "=== Checking Echo ==="
echo "success"

echo "=== Checking Docker ==="
docker stats --no-stream

echo "=== Success ==="

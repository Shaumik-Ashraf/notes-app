#!/usr/bin/env -S bash -e
#
# Setup Ruby on Rails application
# Requirements:
#   mise-en-place version manager installed
#   Ruby 3.4.9 installable with mise (uses ruby-build)

MISE="$HOME/.local/bin/mise"
RAILS_ENV=production
RACK_ENV=production
BUNDLE_DEPLOYMENT=true
BUNDLE_GEMFILE="$HOME/Gemfile.lock"

echo "=== Environment ==="
echo "HOME: $HOME"
echo "MISE: $MISE"
echo "RAILS_ENV: $RAILS_ENV"
echo "BUNDLE_DEPLOYMENT: $BUNDLE_DEPLOYMENT"
echo "BUNDLE_GEMFILE: $BUNDLE_GEMFILE"

echo "=== Running Bundle Install ==="
$MISE exec ruby@3.4.9 -- bundle --gemfile "$HOME/Gemfile"

echo "=== Preparing Database ==="
$MISE exec ruby@3.4.9 -- rails db:create

echo "=== Compiling Assets ==="
$MISE exec ruby@3.4.9 -- rails assets:precompile

echo "=== Setup Done ==="


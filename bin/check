#!/usr/bin/env -S bash -e
#
# Check that OS dependencies are in place

MISE="$HOME/.local/bin/mise"

echo "=== Checking Echo ==="
echo "success"

echo "=== Checking mise ==="
$MISE --version

echo "=== Checking ruby ==="
$MISE exec ruby@3.4.9 -- ruby --version
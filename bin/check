#!/usr/bin/env -S bash -e
#
# Check that OS dependencies are in place

echo "=== Checking Echo ==="
echo "success"

echo "=== Checking mise ==="
.local/bin/mise --version

echo "=== Checking ruby ==="
.local/bin/mise exec ruby@3.4.9 -- ruby --version